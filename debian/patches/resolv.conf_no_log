--- a/pppd/ipcp.c
+++ b/pppd/ipcp.c
@@ -2155,7 +2155,9 @@ create_resolv(u_int32_t peerdns1, u_int3
 
     f = fopen(PPP_PATH_RESOLV, "w");
     if (f == NULL) {
+/*
 	error("Failed to create %s: %m", PPP_PATH_RESOLV);
+*/
 	return;
     }
 
